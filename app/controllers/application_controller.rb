class ApplicationController < ActionController::Base
  #Sessions helper methods can be used throughout the all generate  controllers (all controllers iherit from application controller.)
  include SessionsHelper
end
